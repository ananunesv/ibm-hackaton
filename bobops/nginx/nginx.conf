events {}

http {
    resolver 127.0.0.11 valid=5s;   # Docker internal DNS; forces runtime resolution

    server {
        listen 80;
        location / {
            set $backend_host backend;
            proxy_pass http://$backend_host:8000;
        }
    }
}
